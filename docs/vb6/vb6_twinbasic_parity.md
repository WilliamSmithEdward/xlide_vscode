# twinBASIC parity with the Excel/VBE oracle corpus

Generated by `syntax_corpus/oracle/twinbasic/run_twinbasic_oracle.mjs` on 2026-09-02
against twinBASIC v0.15.983 (twinBASIC_IDE_BETA_983), with the Excel and Office type libraries referenced;
regenerate rather than edit.

Each VBE-verified case was staged as a twinBASIC project and built headlessly.
The expectation compared is the case at COMPILE time: a VBE runtime case
compiled before it ran, so it counts as accepted here. twinBASIC is a
superset of VB6 with published incompatibilities: agreement is evidence that
the oracle can stand in for VB6 on that case, disagreement is a recorded
difference, and neither is a VB6 verdict.

## Matrix

| VBE expected (compile) | twinBASIC accepted | twinBASIC rejected |
| --- | --- | --- |
| accepted | 261 | 3 |
| rejected | 59 | 95 |

Agreement: 356 of 418 (85.2%).
Language-only cases (naming no Excel object): 337 of 391 agree (86.2%); Excel-bound cases: 19 of 27 agree.
Not evidence: 0 case(s) (none).

## Controls

- before control_accept: accepted (expected accepted)
- before control_reject: rejected (expected rejected)
- after control_accept: accepted (expected accepted)
- after control_reject: rejected (expected rejected)

## Disagreements

- member_attribute_after_header_addfromstring_compile: VBE rejected, twinBASIC accepted (build). Compile probe: VBE CodeModule.AddFromString/live-style source rejects a visible member Attribute line immediately after the procedure header. Exported-file import parsing is a separate source mode.
- member_attribute_after_executable_addfromstring_compile: VBE rejected, twinBASIC accepted (build). Compile probe: VBE CodeModule.AddFromString/live-style source rejects a visible member Attribute line after executable procedure body statements.
- indented_attribute_inside_procedure_compile: VBE rejected, twinBASIC accepted (build). Compile probe: an indented Attribute line inside a procedure body represents live-editor-style invalid placement rather than exported metadata.
- optional_long_numeric_default_compile: VBE accepted, twinBASIC rejected (build_failed). Observe whether a numeric literal default is accepted for an Optional Long parameter.
- optional_long_nonnumeric_string_default_compile: VBE rejected, twinBASIC accepted (build). Observe whether a nonnumeric string literal default is rejected for an Optional Long parameter.
- optional_boolean_nonnumeric_string_default_compile: VBE rejected, twinBASIC accepted (build). Observe whether a nonnumeric string literal default is rejected for an Optional Boolean parameter.
- explicit_call_doevents_bare_compile: VBE rejected, twinBASIC accepted (build). DoEvents is a runtime function that VBE rejects as an explicit Call target without parentheses.
- explicit_call_doevents_parentheses_compile: VBE rejected, twinBASIC accepted (build). DoEvents is a runtime function that VBE rejects as an explicit Call target even with empty parentheses.
- standalone_doevents_empty_parentheses_compile: VBE rejected, twinBASIC accepted (build). A standalone DoEvents statement uses empty parentheses without Call.
- suffix_integer_pct_overflow_literal_compile: VBE rejected, twinBASIC accepted (build). Probe: an Integer-suffixed decimal literal above the Integer range (40000%); MS-VBAL predicts a compile error.
- suffix_long_amp_overflow_literal_compile: VBE rejected, twinBASIC accepted (build). Probe: a Long-suffixed decimal literal above the Long range (3000000000&); MS-VBAL predicts a compile error.
- suffix_integer_pct_exact_over_compile: VBE rejected, twinBASIC accepted (build). Probe: the positive Integer-suffixed literal exactly one above the Integer max (32768%).
- suffix_integer_pct_negated_min_compile: VBE rejected, twinBASIC accepted (build). Probe (no-FP boundary): a negated Integer-suffixed literal at the Integer min (-32768%); decides whether the 32768% token is always invalid or valid only when negated.
- suffix_long_amp_negated_min_compile: VBE rejected, twinBASIC accepted (build). Probe (no-FP boundary): a negated Long-suffixed literal at the Long min (-2147483648&).
- suffix_long_amp_glued_concat_accepted: VBE accepted, twinBASIC rejected (build_failed). Probe (no-FP boundary): an over-range digit run glued to & then a string literal (3000000000&"x"). VBE reads & as concatenation (not a Long suffix) because 3000000000 overflows Long, so it is accepted; the suffixed-literal-overflow rule must therefore exclude the & suffix.
- instr_named_zero_start_runtime: VBE rejected, twinBASIC accepted (build). Discovery probe: InStr named Start zero may be a deterministic invalid procedure call.
- dim_type_suffix_as_clause_compile: VBE rejected, twinBASIC accepted (build). Compile probe: a variable declaration cannot combine a type-declaration character with an As clause.
- const_type_suffix_as_clause_compile: VBE rejected, twinBASIC accepted (build). Compile probe: Const declaration with a type-declaration character and an As clause is rejected.
- type_field_type_suffix_as_clause_compile: VBE rejected, twinBASIC accepted (build). Compile probe: UDT field with a type-declaration character and an As clause is rejected.
- function_type_suffix_as_clause_compile: VBE rejected, twinBASIC accepted (build). Compile probe: a Function declaration cannot combine a type-declaration character with an As clause.
- class_public_array_declaration_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). A public array declaration in a class module is rejected before it can become an object member.
- fixed_length_string_length_zero_compile: VBE rejected, twinBASIC accepted (build). A fixed-length String declaration with length 0.
- fixed_length_string_length_above_max_compile: VBE rejected, twinBASIC accepted (build). A fixed-length String declaration just above the MS-VBAL 65,526-character upper bound.
- fixed_length_string_const_length_above_max_compile: VBE rejected, twinBASIC accepted (build). A fixed-length String declaration whose Const integer literal alias resolves above the accepted maximum.
- class_public_type_declaration_compile: VBE rejected, twinBASIC accepted (build). A public user-defined Type declaration in a class module is rejected before it can become an object member.
- class_public_declare_declaration_compile: VBE rejected, twinBASIC accepted (build). A public Declare statement in a class module is rejected before it can become an object member.
- thisworkbook_unknown_member_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). The built-in ThisWorkbook receiver uses a member name that does not exist on the workbook object or source document module.
- workbook_event_member_call_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). A Workbook event name is called as though it were an object method.
- implicit_member_call_parentheses_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). A standalone object member call uses parentheses without the Call keyword.
- standalone_zero_arg_method_call_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). A standalone zero-argument object method call uses empty parentheses without Call.
- standalone_range_property_empty_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). A standalone Range property access uses an empty parenthesized argument list.
- for_each_long_control_variable_compile: VBE rejected, twinBASIC accepted (build). Compile probe: a For Each control variable declared as an intrinsic Long is rejected.
- for_each_array_control_variable_compile: VBE rejected, twinBASIC accepted (build). Compile probe: an array variable cannot be used as a For Each control variable.
- conditional_const_after_procedure_active_elseif_compile: VBE rejected, twinBASIC accepted (build). Compile probe: a valid #ElseIf conditional branch whose active declaration appears after a procedure is still rejected as a misplaced module declaration.
- conditional_elseif_after_else_compile: VBE rejected, twinBASIC accepted (build). Compile probe: #ElseIf cannot appear after #Else in the same conditional-compilation block.
- conditional_duplicate_else_compile: VBE rejected, twinBASIC accepted (build). Compile probe: only one #Else branch is allowed in a conditional-compilation block.
- if_elseif_after_else_compile: VBE rejected, twinBASIC accepted (build). Compile probe: ElseIf cannot appear after Else in the same If block.
- duplicate_parameter_name_compile: VBE rejected, twinBASIC accepted (build). Observe whether a procedure signature that repeats a parameter name is rejected at compile time (expected: Ambiguous name detected / Duplicate declaration).
- byval_paramarray_compile: VBE accepted, twinBASIC rejected (build_failed). Observe whether a ParamArray parameter declared ByVal is rejected at compile time (ParamArray must be a ByRef Variant array).
- empty_type_block_compile: VBE rejected, twinBASIC accepted (build). Observe whether a Type (UDT) block declaring no members is rejected at compile time.
- duplicate_option_explicit_compile: VBE rejected, twinBASIC accepted (build). Observe whether a module header that repeats Option Explicit is rejected at compile time (duplicate Option statement).
- duplicate_case_else_compile: VBE rejected, twinBASIC accepted (build). Observe whether a Select Case block with two Case Else branches is rejected at compile time.
- corpus_friend_003_compile: VBE rejected, twinBASIC accepted (build). Corpus FRIEND_003 oracle discovery: Friend is only valid in object modules
- corpus_withevents_003_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). Corpus WITHEVENTS_003 oracle discovery: WithEvents not valid in standard module
- corpus_implements_003_compile: VBE rejected, twinBASIC accepted (build). Corpus IMPLEMENTS_003 oracle discovery: Implements only valid in class/object modules
- corpus_event_004_compile: VBE rejected, twinBASIC accepted (build). Corpus EVENT_004 oracle discovery: unsure
- corpus_api_vis_003_compile: VBE rejected, twinBASIC accepted (build). Corpus API_VIS_003 oracle discovery: public member exposes private type (Public Sub parameter typed as a module-local Private user-defined type)
- corpus_prop_005_compile: VBE rejected, twinBASIC accepted (build). Corpus PROP_005 oracle discovery: rejected
- corpus_cls_010_compile [Excel-bound]: VBE rejected, twinBASIC accepted (build). Corpus CLS_010 oracle discovery: rejected
- corpus_tok_003_compile: VBE rejected, twinBASIC accepted (build). Corpus TOK_003 oracle discovery: rejected
- corpus_type_003_compile: VBE rejected, twinBASIC accepted (build). Corpus TYPE_003 oracle discovery: rejected
- corpus_arg_limit_001b_compile: VBE rejected, twinBASIC accepted (build). Corpus ARG_LIMIT_001B oracle discovery: Too many arguments
- corpus_array_limit_001b_compile: VBE rejected, twinBASIC accepted (build). Corpus ARRAY_LIMIT_001B oracle discovery: Too many dimensions
- corpus_name_rule_002_compile: VBE rejected, twinBASIC accepted (build). Corpus NAME_RULE_002 oracle discovery: Expected: identifier
- corpus_name_limit_001b_compile: VBE rejected, twinBASIC accepted (build). Corpus NAME_LIMIT_001B oracle discovery: Identifier too long
- corpus_line_limit_001b_compile: VBE rejected, twinBASIC accepted (build). Corpus LINE_LIMIT_001B oracle discovery: Line too long
- corpus_edges_002_deftype_in_proc_compile: VBE rejected, twinBASIC accepted (build). Corpus EDGES_002_DEFTYPE_IN_PROC oracle discovery: Invalid inside procedure
- corpus_canary_006_compile: VBE rejected, twinBASIC accepted (build). Corpus CANARY_006 oracle discovery: unsure
- corpus_bp_nonopt_pubudt_param: VBE rejected, twinBASIC accepted (build). Binder-rule boundary probe: corpus_bp_nonopt_pubudt_param
- corpus_bp_priv_udt_privproc_param: VBE rejected, twinBASIC accepted (build). Binder-rule boundary probe: corpus_bp_priv_udt_privproc_param
- exit_function_in_property_let: VBE rejected, twinBASIC accepted (build). Exit Function inside a Property Let. Property Let/Set are not value-returning, so VBE rejects Exit Function.
- exit_sub_in_property_let: VBE rejected, twinBASIC accepted (build). Exit Sub inside a Property Let. VBE rejects Exit Sub inside a Property Let.

## Not evidence

- none
